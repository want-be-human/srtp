# ============================================
# 焊育智眸后端依赖 - 版本锁定确保兼容性
# ============================================

# Web框架
fastapi==0.109.0
uvicorn[standard]==0.27.0

# 数据库
sqlalchemy==2.0.25

# 文件处理
python-multipart==0.0.6

# YOLO检测
ultralytics==8.0.196
# 项目自带的 best.pt 是用 dill 序列化的，ultralytics 自身没列 dill 为依赖，
# 但加载 .pt 必须；显式锁定避免 ultralytics AutoInstall（官方将移除该机制）。
dill>=0.3.7

# AI API (版本兼容)
openai==1.12.0
httpx==0.26.0

# 环境变量
python-dotenv==1.0.0

# 数据处理
pandas==2.1.4
numpy==1.26.3
scikit-learn==1.3.2

# 图表生成
matplotlib==3.8.2
seaborn==0.13.2

# 图像处理
opencv-python==4.9.0.80

# 登录密码哈希
bcrypt>=4.0,<5.0

# PDF生成
reportlab==4.0.8

# HTTP请求
requests==2.31.0

# ============================================
# 安装说明:
# pip install -r requirements.txt
#
# 如遇网络问题，可使用国内镜像:
# pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# ============================================
